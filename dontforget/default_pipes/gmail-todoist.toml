[source]
class = "gmail"
user = "{{ env.GMAIL_USER }}"
password = "{{ env.GMAIL_PASSWORD }}"
mark_read = true
archive = true

[target]
class = "todoist"
api_token = "{{ env.TODOIST_API_TOKEN }}"
id = "{{ gmail.uid }}"
url = "{{ gmail.url }}"
content = "{{ gmail.subject }}"
